version: '3.8'

services:
  rasat-analyzer:
    build: .
    container_name: rasat_scoring
    volumes:
      - .:/app
      # เชื่อมต่อ X11 เพื่อให้แสดงผล GUI บนหน้าจอเครื่องได้
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY:-:0}
    network_mode: "host"